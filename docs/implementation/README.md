# Implementation Directory

This directory stores live execution state, especially the active step and next allowed action.

This directory is for current state only.

It should not absorb the full verification history of completed steps.

See the root `README.md` for the full explanation and rules.
