# Verification Directory

This directory stores evidence-based completion records for implementation steps.

Each completed step should have its own verification file, for example:

- `step-01-verification.md`
- `step-02-verification.md`

Do not replace these files with summary text inside `implementation-state.md`.

See the root `README.md` for the full explanation and rules.
