# Claude Adapter

The Claude adapter documents how the harness is exposed through `CLAUDE.md` and `.claude/skills/`. It must contain tool-specific integration details only. Shared orchestration rules belong in `rules/` and shared skill content belongs in `skills-src/`.

