# Codex Adapter

The Codex adapter documents how the harness is exposed through `AGENTS.md` and `.agents/skills/`. It must contain tool-specific integration details only. Shared orchestration rules belong in `rules/` and shared skill content belongs in `skills-src/`.

