# Examples

This directory holds sample inputs and reference artifacts that are useful for testing the harness without polluting live operational state.

## Rules

- Keep example requirements here instead of `docs/requirements/`
- Do not treat files here as the current source of truth
- Use these files only for dry runs, demos, and regression-style harness checks
